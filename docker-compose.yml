version: '3.8'

services:
  calendar-agent:
    build: .
    container_name: calendar-agent
    volumes:
      - ./logs:/app/logs
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - TZ=${TZ:-America/New_York}
